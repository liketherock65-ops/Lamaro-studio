const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));
const path=location.pathname.split('/').pop()||'index.html';const current=path==='index.html'?'home':path.replace('.html','');document.querySelectorAll(`[data-nav="${current}"]`).forEach(e=>e.classList.add('active'));
const menu=document.querySelector('.menu'),panel=document.querySelector('.mobileMenu'),shade=document.querySelector('.menuShade'),close=document.querySelector('.closeMenu');function setMenu(open){panel?.classList.toggle('open',open);shade?.classList.toggle('open',open);document.body.style.overflow=open?'hidden':'';menu?.setAttribute('aria-expanded',String(open));panel?.setAttribute('aria-hidden',String(!open))}menu?.addEventListener('click',()=>setMenu(true));close?.addEventListener('click',()=>setMenu(false));shade?.addEventListener('click',()=>setMenu(false));panel?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>setMenu(false)));document.addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)});
document.querySelectorAll('img').forEach(img=>img.addEventListener('error',()=>{img.src='assets/store.svg';img.style.objectFit='contain';img.style.padding='30px'},{once:true}));
document.querySelectorAll('.gallery img,.cardImg img,.heroCard img,.imageFrame img').forEach(img=>img.addEventListener('click',()=>{const box=document.createElement('div');box.className='lightbox';box.innerHTML=`<button aria-label="Close">×</button><img src="${img.src}" alt="${img.alt||'Lamaro Studio'}"><span>Lamaro Studio · concept visual</span>`;document.body.appendChild(box);requestAnimationFrame(()=>box.classList.add('show'));box.addEventListener('click',e=>{if(e.target===box||e.target.tagName==='BUTTON')box.remove()})}));
